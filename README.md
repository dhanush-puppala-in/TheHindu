# TheHindu
An admin-controlled Node.js scraping bot that extracts latest news articles from The Hindu, stores them in MongoDB with duplicate protection, and supports manual &amp; scheduled scraping using cron jobs.
